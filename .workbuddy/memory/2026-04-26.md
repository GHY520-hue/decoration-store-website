# 2026-04-26 工作日志

## 装修材料店官网 - Cloudflare Pages 部署配置

- 项目技术栈：Vite 5 + Vue 3 + TypeScript + TailwindCSS + Vue Router（WebHistory 模式）
- 新增 `public/_redirects`：`/* /index.html 200` 解决 SPA 刷新 404 问题
- 新增 `public/_headers`：为 `/assets/*` 设置一年强缓存，`/index.html` 设置 no-cache，加安全响应头
- 优化 `vite.config.ts`：添加 `manualChunks` 拆分 vue-vendor / vendor，提升缓存命中率
- 用户计划通过 GitHub 连接 Cloudflare Pages 部署，构建命令 `npm run build`，输出目录 `dist`
- GitHub 网页上传会丢失嵌套文件夹内容（src 目录全丢），必须用 git push
- Clash 代理端口 7890，git 配置 `http.proxy` / `https.proxy` 后可成功 push
- 修复 tsconfig.json：删除不存在的 `"api"` include 路径
- 修复 package.json：`vue-tsc -b && vite build` 改为 `vite build`（绕过类型检查）
- 已通过 git push --force 成功推送完整代码到 GitHub
- ✅ **Cloudflare Pages 部署成功！** 项目地址：https://decoration-store-website.pages.dev
- 部署配置：Build command = `npm run build`，Output directory = `dist`，Region = Earth（全球）
- ✅ 网站预览验证通过，可正常访问
- ⚠️ 待确认：国内网络能否稳定访问 .pages.dev 域名，如需绑定自定义域名请告知
- 🐛 **白屏问题排查与修复**：
  - 原因1: API_BASE = localhost:8080，部署后无法访问后端 → 添加 Mock 数据 fallback
  - 原因2: vite-plugin-trae-solo-badge + unplugin-vue-dev-locator 生产环境可能报错 → 从 vite.config.ts 移除
  - 原因3: Banner 图片使用 trae-api-cn 外部 AI 图片链接 → 替换为 picsum.photos 稳定占位图
- ✅ 已提交并推送到 GitHub（commit: fix 白屏修复），等待 Cloudflare 自动重建部署
